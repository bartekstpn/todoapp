stages:
  - check

check_readme:
  stage: check
  script:
    - echo "Sprawdzam, czy README.md istnieje..."
    - test -f README.md || (echo "Brak README.md!" && exit 1)
    - echo "Plik istnieje. Oto jego pierwsze 5 linijek:"
    - head -n 5 README.md
  only:
    - main